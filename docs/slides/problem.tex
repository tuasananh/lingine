\section{Bài Toán}

\begin{frame}{Tổng quan đề tài}
  \begin{columns}[T,totalwidth=\textwidth]
    \begin{column}{0.45\textwidth}
  \vspace{0.5cm}
      \only<-5>{\begin{itemize}[<+->]
        \item Xây dựng AI chơi Cờ Tướng.
        \item AI được mô hình hóa như một Intelligent Agent.
        \item Quan sát và phân tích trạng thái bàn cờ.
        \item Đưa ra nước đi tốt nhất trong môi trường đối kháng.
        \item Một trong những bài toán kinh điển của AI: Game playing.
      \end{itemize}}
      \only<6->{
        \begin{itemize}
          \item<6-> Cờ Tướng là bài toán:
            \begin{enumerate}
              \item Hai người chơi.
              \item Đối kháng Zero-Sum.
            \end{enumerate}
          \item<7-> Không gian tìm kiếm rất lớn ($\approx 10^{150}$)
          \item<8-> Vì vậy, đòi hỏi:
            \begin{enumerate}
              \item Tìm kiếm thông minh.
              \item Đánh giá chính xác.
              \item Hiệu năng cao.
            \end{enumerate}
        \end{itemize}
      }
    \end{column}%
    \begin{column}{0.45\textwidth}
      \begin{figure}
        \centering
        \includegraphics[height=0.64\textheight]{\figpath{fig-00-board-start.png}}
        \caption{Bàn cờ khởi đầu.}
      \end{figure}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}[t]{Kiến trúc hệ thống}
  \begin{columns}[T,totalwidth=\textwidth]
    \begin{column}{0.4\textwidth}
      \begin{block}{Agent}
        \textbf{Đầu vào}
        \begin{itemize}[<+->]
          \item<2-> Trạng thái hiện tại của bàn cờ.
          \item<3-> Lượt chơi hiện tại.
          \item<4-> Chuỗi các nước đi.
      \end{itemize}
        \onslide<5->{\textbf{Đầu ra}}
        \begin{itemize}[<+->]
          \item<6-> Nước đi tốt nhất cho trạng thái bàn cờ hiện tại.
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.6\textwidth}
      \begin{figure}[htbp]
        \centering
        \definecolor{blueFill}{HTML}{E1EDFF}
        \definecolor{blueStroke}{HTML}{2B70E4}
        \definecolor{greenFill}{HTML}{E6F9EC}
        \definecolor{greenStroke}{HTML}{28A745}
        \definecolor{redFill}{HTML}{FFEBEB}
        \definecolor{redStroke}{HTML}{DC3545}
        \begin{tikzpicture}[
            scale=0.5,
            transform shape,
            % --- Node Styles ---
            blueNode/.style={
              rectangle, 
              draw=blueStroke, 
              fill=blueFill, 
              line width=2pt, 
              rounded corners=4pt,
              align=center,
              inner sep=10pt,
            },
            greenNode/.style={
              rectangle, 
              draw=greenStroke, 
              fill=greenFill, 
              line width=2pt, 
              rounded corners=4pt,
              align=center,
              inner sep=10pt,
            },
            redNode/.style={
              rectangle, 
              draw=redStroke, 
              fill=redFill, 
              line width=2pt, 
              dashed,
              rounded corners=4pt,
              align=center,
              inner sep=10pt,
            },
            % --- Fixed Cylinder Style ---
            dbNode/.style={
              cylinder, 
              draw=greenStroke, 
              fill=greenFill, 
              line width=2pt, 
              shape border rotate=90, 
              align=center, 
              inner sep=10pt,            % Reduced inner padding
              aspect = 0.25
            },
            % --- Arrow Styles ---
            arrow/.style={
              -{Stealth}, 
              draw=black!80
            },
            dashedArrow/.style={
              -{Stealth}, 
              dashed, 
              draw=black!80
            },
            biArrow/.style={
              {Stealth}-{Stealth}, 
              draw=black!80
            },
            ]

            %% --- Nodes Placement ---
            \onslide<1-> \node (GUI) [blueNode] at (0,-0.92) {GUI};
            \onslide<2-> \node (ThreadA) [blueNode, right=of GUI, xshift=1.5cm] {Luồng A: Stdin Listener};
            \onslide<3-> \node (Engine) [greenNode, below=of ThreadA] {Luồng B: UCI Engine};

            % Right Side Node
            \onslide<5-> \node (State) [redNode, right=of ThreadA, xshift=1.5cm] {Cờ trạng\\thái};

            % Core Engine Stack
            \onslide<4-> \node (ID) [greenNode, below=of Engine] {Iterative Deepening};
            \onslide<4-> \node (Algo) [greenNode, below=of ID, xshift=1.5cm] {Negamax Alpha-Beta};

            % Bottom & Left Nodes
            \onslide<4-> \node (Eval) [greenNode, below=of Algo] {Hàm đánh giá tĩnh};
            \onslide<4-> \node (Trans) [dbNode, left=2.2cm of Algo] {Transposition\\Table}; % Adjusted spacing slightly for balance

            %% --- Connections & Labels ---
            % GUI -> ThreadA
            \onslide<2-> \draw [arrow] (GUI) -- node[below, font=\small, align=center] {Lệnh\\qua stdin} (ThreadA);

            % ThreadA -> Engine
            \onslide<3-> \draw [arrow] (ThreadA) -- node[left, font=\small] {Chuyển lệnh} (Engine);

            % Engine -> GUI (Loop back loop on the left side)
            \onslide<6-> \draw [arrow] (Engine.west) .. controls +(-3.5,0) and +(0,0) .. node[font=\small, align=center, below, yshift=-0.25cm] {Trả về\\bestmove} (GUI.south);

            % ThreadA -> State
            \onslide<5-> \draw [arrow] (ThreadA) -- node[below, font=\small, align=center] {Ngắt khẩn\\cấp} (State);

            % Engine -> ID -> Algo
            \onslide<4-> \draw [arrow] (Engine) -- (ID);
            \onslide<4-> \draw [arrow] (ID) -- (Algo);

            % State -> Algo (Dashed routing down to the side of Algo)
            \onslide<5-> \draw [dashedArrow] (State.south) -- node[font=\small, align=left, right, xshift=0.2cm] {Đọc liên tục\\để dừng} (Algo.east);

            % Algo -> Eval
            \onslide<4-> \draw [arrow] (Algo) -- node[midway, right, font=\small] {} (Eval);

            % Algo <-> Trans (Bidirectional)
            \onslide<4-> \draw [biArrow] (Algo) -- node[midway, below, font=\small, align=center] {Lưu\\/Đọc thế} (Trans);

          \end{tikzpicture}
          \caption{Sơ đồ kiến trúc của Lingine}
        \end{figure}

    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Giao thức UCI}
  \begin{figure}
      \centering
        \begin{tikzpicture}[
            scale=0.92,
            transform shape,
            % Styles for lifelines and boxes
            guiBox/.style={draw=lingineBlue, fill=lingineBlue!10, thick, rounded corners=2pt, minimum width=2.0cm, minimum height=0.6cm, align=center, font=\small},
            engBox/.style={draw=lingineGreen, fill=lingineGreen!10, thick, rounded corners=2pt, minimum width=2.0cm, minimum height=0.6cm, align=center, font=\small},
            lifeline/.style={draw=black!30, dashed, thick},
            % Arrow styles
            toEngine/.style={-{Stealth}, draw=lingineBlue, thick},
            toGui/.style={-{Stealth}, draw=lingineGreen, thick},
            infoLine/.style={-{Stealth}, draw=black!40, dashed, thick},
            % Text style
            lbl/.style={font=\scriptsize\ttfamily, midway, above, yshift=-1pt}
          ]
          
          % Lifeline Headers
          \node[guiBox] (GUI) at (0,0) {Chess GUI};
          \node[engBox] (Engine) at (7.5,0) {Lingine (Engine)};

          \path let \p1 = (GUI) in 
            \pgfextra{\xdef\guix{\x1}}; % Store x-coordinate in a macro
          \path let \p1 = (Engine) in 
            \pgfextra{\xdef\enginex{\x1}}; % Store x-coordinate in a macro
          
          % Lifeline Paths
          \coordinate[below=4.8cm of GUI] (GUIbottom);
          \coordinate[below=4.8cm of Engine] (Enginebottom);
          \draw[lifeline] (GUI) -- (GUIbottom);
          \draw[lifeline] (Engine) -- (Enginebottom);
          
          % Communication Steps
          % Step 1: Handshake
         \onslide<2-> \draw[toEngine] (\guix,-0.7) -- node[lbl] {uci} (\enginex,-0.7);
          \onslide<3-> \draw[toGui] (\enginex,-1.3) -- node[lbl] {uciok} (\guix,-1.3);
          
          % Step 2: Ready Check
          \onslide<4-> \draw[toEngine] (0,-2.0) -- node[lbl] {isready} (\enginex,-2.0);
          \onslide<5-> \draw[toGui] (\enginex,-2.6) -- node[lbl] {readyok} (0,-2.6);
          
          % Step 3: Calculation Flow
       \onslide<6->    \draw[toEngine] (\guix,-3.3) -- node[lbl] {position startpos moves ...} (\enginex,-3.3);
          \onslide<7-> \draw[toEngine] (\guix,-3.9) -- node[lbl] {go depth 10} (\enginex,-3.9);
          
          % Step 4: Search Info & Bestmove
 \onslide<8->          \draw[infoLine] (\enginex,-4.5) -- node[lbl] {info depth 1 ...} (\guix,-4.5);
        \onslide<9->  \draw[toGui] (\enginex,-5.0) -- node[lbl] {bestmove h0g2} (\guix,-5.0);
          
        \end{tikzpicture}
      \caption{Luồng giao tiếp qua giao thức UCI}
    \end{figure}
\end{frame}

% Định nghĩa cột mới tự động canh lề trái, tự giãn độ rộng và thêm \hspace{0pt} để sửa lỗi baseline của \onslide


\begin{frame}{Các trở ngại khi lập trình cờ tướng}
  \begin{columns}[T,totalwidth=\textwidth]
    \begin{column}{0.54\textwidth}
      \small
      \begin{tabular}{L{2.5cm} L{4.5cm}}
        \toprule
        \textbf{Cấu trúc} & \textbf{Đặc thù} \\
        \midrule
        \onslide<2-> 90 ô & Không gian tìm kiếm rất lớn \\
        \onslide<3-> Pháo & Nước đi phụ thuộc quân cản \\
        \onslide<4-> Cản chân & Giảm sự linh hoạt của Mã và Tượng \\
        \onslide<5-> Hàm đánh giá & Phức tạp, yêu cầu nhiều kiến thức cho tàn cuộc \\
        \onslide<6-> Bộ luật đặc biệt & Lộ mặt Tướng, chiếu/đuổi dai \\
        \bottomrule
      \end{tabular}
    \end{column}%
    \begin{column}{0.42\textwidth}
      \onslide<1-> \begin{figure}
        \centering
        \includegraphics[height=0.54\textheight]{\figpath{fig-01-blocking.png}}
        \caption{Cản mắt tượng và chân mã.}
    \end{figure}
    \end{column}
  \end{columns}
\end{frame}



